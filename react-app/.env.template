# =======================================================
# MyFolio - React Frontend Environment Configuration
# =======================================================
# 
# Instructions:
# 1. Copy this file to `.env` in the same directory
# 2. Replace all placeholder values with your actual values
# 3. Never commit the `.env` file to version control
#

# API Configuration
VITE_API_BASE_URL=http://localhost:8000
VITE_API_VERSION=v1

# Development Settings
VITE_ENVIRONMENT=development
VITE_DEBUG=true

# =======================================================
# Setup Instructions:
# =======================================================
#
# 1. Backend API:
#    - Make sure your FastAPI backend is running on port 8000
#    - Update VITE_API_BASE_URL if using different port/host
#
# 2. Google OAuth:
#    - The OAuth flow starts from the frontend but processes on backend
#    - Make sure VITE_GOOGLE_OAUTH_URL matches your backend OAuth endpoint
#
# 3. Gemini AI (Optional):
#    - Go to https://aistudio.google.com/app/apikey
#    - Create a new API key
#    - Add it to VITE_GEMINI_API_KEY for AI features
#
# =======================================================
