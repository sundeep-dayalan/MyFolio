# Environment Configuration Example
# Copy this file to .env and update with your values
# 
# LOCAL DEVELOPMENT SETUP:
# - Points to DEV environment in Azure Key Vault
# - Uses DEV Cosmos DB database (sage-dev-db)
# - No local Key Vault/Cosmos needed - connects to Azure dev resources
#
# PRODUCTION DEPLOYMENT:
# - Uses PROD Key Vault secrets automatically
# - Uses PROD Cosmos DB database (sage-prod-db)

# Environment (determines which Key Vault secrets to use)
ENVIRONMENT=development  # development = uses dev-* secrets, production = uses prod-* secrets
DEBUG=true
LOG_LEVEL=DEBUG

# Azure Key Vault URL (required for both dev and prod)
KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/

# Application Settings (non-sensitive, same for all environments)
PROJECT_NAME="Sage API"
VERSION=2.0.2
API_V1_PREFIX=/api/v1
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
PLAID_ENV=sandbox

# CORS Settings (for local development)
FRONTEND_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
ALLOWED_HOSTS=localhost,127.0.0.1

# OAuth Redirect (for local development)
AZURE_REDIRECT_URI=http://localhost:8000/api/v1/auth/oauth/microsoft/callback

# ══════════════════════════════════════════════════════════════
# SENSITIVE VALUES - STORED IN AZURE KEY VAULT (DO NOT SET HERE)
# ══════════════════════════════════════════════════════════════
# These are automatically loaded from Key Vault based on ENVIRONMENT:
# 
# Development (ENVIRONMENT=development):
#   - dev-secret-key
#   - dev-azure-client-id  
#   - dev-azure-client-secret
#   - dev-azure-tenant-id
#   - dev-plaid-client-id
#   - dev-plaid-secret
#   - dev-cosmos-db-endpoint
#   - dev-cosmos-db-key
#   - dev-cosmos-db-name (sage-dev-db)
#
# Production (ENVIRONMENT=production):
#   - prod-secret-key
#   - prod-azure-client-id
#   - prod-azure-client-secret  
#   - prod-azure-tenant-id
#   - prod-plaid-client-id
#   - prod-plaid-secret
#   - prod-cosmos-db-endpoint
#   - prod-cosmos-db-key
#   - prod-cosmos-db-name (sage-prod-db)
# ══════════════════════════════════════════════════════════════

# For first-time setup, you can temporarily set these here, but they
# should be moved to Key Vault for security:
# SECRET_KEY=temporary-dev-key-replace-in-keyvault
# AZURE_CLIENT_ID=temporary-client-id-replace-in-keyvault
# AZURE_CLIENT_SECRET=temporary-secret-replace-in-keyvault
# AZURE_TENANT_ID=your-tenant-id
# PLAID_CLIENT_ID=your-plaid-client-id
# PLAID_SECRET=your-plaid-secret
# COSMOS_DB_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
# COSMOS_DB_KEY=your-cosmos-primary-key
# COSMOS_DB_NAME=sage-dev-db
