# =======================================================
# Sage - React Frontend Environment Configuration (Azure)
# =======================================================
# 
# Instructions:
# 1. Copy this file to `.env.development` for local development
# 2. Replace all placeholder values with your actual values
# 3. For production, create `.env.production` with production values
#

# Azure Functions API Configuration
VITE_API_BASE_URL=http://localhost:7071/api

# Application Environment
VITE_APP_ENV=development

# Google OAuth Client ID (get from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# =======================================================
# Setup Instructions:
# =======================================================
#
# 1. Azure Functions Backend:
#    - Make sure your Azure Functions are running on port 7071 (local)
#    - Update VITE_API_BASE_URL for production deployment
#
# 2. Google OAuth:
#    - Go to Google Cloud Console
#    - Create OAuth 2.0 credentials
#    - Add your domain to authorized origins
#    - Use the Client ID in VITE_GOOGLE_CLIENT_ID
#
# 3. Production Deployment:
#    - Azure Static Web Apps will automatically set VITE_API_BASE_URL
#    - Make sure to configure VITE_GOOGLE_CLIENT_ID in Azure portal
#
# =======================================================
